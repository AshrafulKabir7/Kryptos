services:
  - type: web
    name: cse721-crypto-backend
    env: python
    buildCommand: pip install -r requirements_web.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
